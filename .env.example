# Neuronet Application Configuration

# Flask Environment (development, testing, or production)
FLASK_ENV=development

# Flask Host and Port
FLASK_HOST=127.0.0.1
PORT=5000

# Secret key for session management
SECRET_KEY=change_for_production

# MySQL Database Configuration (optional, SQLite used if not provided)
# Only required for production deployments
# MYSQL_PASSWORD=your_mysql_password_here
# MYSQL_HOST=localhost

# Database name (default: AetherVoid)
# DB_NAME=AetherVoid
